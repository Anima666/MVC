@{
    ViewBag.Title = "Home Page";
}
<center><h1>Камень, ножницы, бумага </h1></center>
<div>
    <ol>
        @foreach (var line in ViewBag.List)
        {
            <li>@line</li>
        }
    </ol>
</div>

<br/>

@*<div>

  <img src="d:\GitRep\MVC\GameKHB\Game_KHB\makas.jpg" width="200" height="200" alt="picture" />

</div>*@

    @using (Html.BeginForm("Accord", "Home", FormMethod.Post))
    {
        
            <div>
                <form class="form-horizontal">

                    <div class="form-group">

                        <label class="col-sm-2 control-label">Введите ваш выбор</label>

                        <div class="col-sm-10">

                            <input name="Answer" />
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-sm-offset-2 col-sm-10">
                            <button type="submit" class="btn btn-success">Accept</button>
                        </div>
                    </div>

                </form>
            </div>
        
    }

    <button type="submit" class="btn btn-success" onclick="ShowHMAC()">Check</button>
    <div style="visibility:hidden" id="JSHmac">
        @if (ViewBag.HMAC != null)
        {
            @ViewBag.HMAC;
        }
    </div>


    <script src="~/Content/ShowHMAC.js"></script>

    <div>
        @if (ViewBag.Result != null)
        {
            @ViewBag.Result;
        }
    </div>
